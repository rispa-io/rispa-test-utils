const PROJECT_NAME = 'rispa-project'
const PLUGIN_TARGET_PATH = './packages/rispa-test-plugin'
const TEST_UTILS_PACKAGE_NAME = '@rispa/test-utils'

module.exports = {
  PROJECT_NAME,
  PLUGIN_TARGET_PATH,
  TEST_UTILS_PACKAGE_NAME,
}
